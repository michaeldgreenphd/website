# Build backend for the two sdist-only pins in requirements.txt (free-proxy
# 1.0.6 and bibtexparser 1.4.4). Installed first, with --require-hashes, and
# the main install then runs with --no-build-isolation, so pip builds those
# two with this exact setuptools instead of fetching the newest one.
setuptools==84.0.0 \
    --hash=sha256:51a52592b3b99e102b609654876bd65f19f999935166d1352678931132b0c670
